# For ARM boards
INSTALL_DTB="${ARTIFACTS_DIR}/dtb"
INSTALL_BOOTLOADER="${ARTIFACTS_DIR}/uboot.bin"

## Uncomment to use GPT (installer image - not live)
#INSTALL_USE_GPT=1

# fsl-ls10xx or xilinx-zynq
BOARD_NAME="fsl-ls10xx" 
